---
import { type CollectionEntry } from "astro:content";
import { SITE } from "@/siteConfig";

interface Props {
  entry: CollectionEntry<"newsletters">;
}

const { entry } = Astro.props;

const newsletterTitle = entry.data.title || SITE.title;
const newsletterDescription = entry.data.description || SITE.description;
const base = SITE.href || Astro.url.origin;
const ogImage = new URL("./og.jpg", base);
---

<title>{`${newsletterTitle} | ${SITE.title}`}</title>
<meta name="title" content={`${newsletterTitle} | ${SITE.title}`} />
<meta name="description" content={newsletterDescription} />
<link rel="canonical" href={new URL(Astro.url.pathname, base)} />

<meta property="og:title" content={newsletterTitle} />
<meta property="og:description" content={newsletterDescription} />
<meta
  property="og:image"
  content={entry?.data?.image?.src
    ? `${base}${entry.data.image.src}`
    : ogImage}
/>
<meta property="og:image:alt" content={newsletterTitle} />
<meta property="og:type" content="article" />
<meta property="og:locale" content={SITE.locale} />

<meta name="twitter:title" content={newsletterTitle} />
<meta name="twitter:description" content={newsletterDescription} />
<meta
  property="twitter:image"
  content={entry?.data?.image?.src
    ? `${base}${entry.data.image.src}`
    : ogImage}
/>
<meta name="twitter:image:alt" content={newsletterTitle} />
<meta name="twitter:card" content="summary_large_image" />

{
  entry?.data.tags &&
    entry.data.tags.map((tag: string) => {
      return <meta property="article:tag" content={tag} />;
    })
}
